/**
 * Sorts an array and returns an object with the sorted array and the corresponding indices.
 * @param array
 * @returns {values, indices}
 */
export declare function sortArray(array: number[]): {
    values: number[];
    indices: number[];
};
//# sourceMappingURL=sortArray.d.ts.map