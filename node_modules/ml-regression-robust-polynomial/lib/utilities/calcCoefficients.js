"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.calcCoefficients = calcCoefficients;
const ml_matrix_1 = require("ml-matrix");
/**
 * @ignore
 * @param tuple
 * @param powers
 * @returns
 */
function calcCoefficients(tuple, powers) {
    const X = [];
    const Y = [];
    for (let i = 0; i < tuple.length; i++) {
        Y[i] = [tuple[i].y];
        X[i] = new Array(powers.length);
        for (let j = 0; j < powers.length; j++) {
            X[i][j] = tuple[i].x ** powers[j];
        }
    }
    return (0, ml_matrix_1.solve)(X, Y).to1DArray();
}
//# sourceMappingURL=calcCoefficients.js.map