import { MetricFunction } from "./types";
export declare class Hdbscan {
    private input;
    private minClusterSize;
    private minSamples;
    private alpha;
    private metric;
    private clusters;
    private noise;
    constructor(input: Array<Array<number>>, minClusterSize?: number, minSamples?: number, alpha?: number, metric?: MetricFunction);
    getClusters(): number[][];
    getNoise(): number[];
}
