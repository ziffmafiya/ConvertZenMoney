export interface Options {
    windowSize: number;
    derivative: number;
    polynomial: number;
    pad: 'none' | 'pre' | 'post';
    padValue: 'replicate' | 'circular' | 'symmetric' | number;
}
export default function savitzkyGolay(data: Array<number>, h: number, options?: Partial<Options>): Array<number>;
