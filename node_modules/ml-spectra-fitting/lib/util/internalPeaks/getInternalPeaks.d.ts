import { Shape1D, Shape1DInstance } from 'ml-peak-shape-generator';
import { Peak, OptimizeOptions } from '../../index';
type Parameter = 'x' | 'y' | 'fwhm' | 'mu' | 'gamma';
type Property = 'init' | 'min' | 'max' | 'gradientDifference';
export interface InternalPeak {
    id?: string;
    shape: Shape1D;
    shapeFct: Shape1DInstance;
    parameters: Parameter[];
    propertiesValues: Record<Property, number[]>;
    fromIndex: number;
    toIndex: number;
}
/**
 * Return an array of internalPeaks that contains the exact init, min, max values based on the options
 * @param peaks
 * @param options
 * @returns
 */
export declare function getInternalPeaks(peaks: Peak[], minMaxY: {
    min: number;
    max: number;
    range: number;
}, options?: OptimizeOptions): InternalPeak[];
export {};
//# sourceMappingURL=getInternalPeaks.d.ts.map